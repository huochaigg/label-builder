/**
 * PrintTagElementPanel 组件
 * 是标签canvas画板的底部面板
 * 内容包含：
 *  | 文字：设置文案，字重，字号，行高，颜色，字体，x，y，对齐，旋转角度，换行宽度……
 *  | 图形：宽，高，
 *  | 二维码：宽，高，内容，x，y，密度
 *  | 条形码：宽，内容，x，y，密度，底部是否显示text，text对齐方式，text字号，text与条码间距
 */
import styles from './style.module.less'

export default function PrintTagElementPanel() {
  return <section className={styles.printTagElementPanel}>
    
  </section>
}